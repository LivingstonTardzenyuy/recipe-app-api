# version: "3.9"  # Docker version  


# services:
#   app:
#     build:
#     args:
#       - DEV=true
#       context: .
#     ports:
#       - "8000:8000"
#     volumns:
#       - ./app:/app
#     command: >
#       sh -c "python manage.py runserver 0.0.0.0:8000"

version: "3.9"  # Docker version  

services:
  app:
    build:
      context: .  
      args:
        DEV: 'true'
    ports:
      - "8000:8000"
    volumes:  
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"